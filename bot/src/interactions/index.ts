/*

    Button, menu and modal interaction files for discord bot

*/

import { InteractionCategory, interactionCategory } from "../interfaces"

export const buttonCategory = [] as InteractionCategory[]

export const menuCategory = [] as InteractionCategory[]

export const modalCategory = [] as InteractionCategory[]
