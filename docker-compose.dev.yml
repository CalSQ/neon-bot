networks:
  app-network:
    driver: bridge
services:
  api:
    build: ./api
    restart: always
    ports:
      - 6001:6001
    env_file:
      - path: ./vault/api.env.development
        required: true
    networks:
      - app-network
  bot:
    build: ./bot
    restart: always
    env_file:
      - ./vault/bot.env.development
    networks:
      - app-network
  redis:
    image: redis:alpine
    restart: always
    ports:
      - 6379:6379
    networks:
      - app-network
  nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/cert.pem:/credentials/cert.pem
      - ./nginx/key.pem:/credentials/key.pem
    networks:
      - app-network
    depends_on:
      - api
volumes:
  data:
